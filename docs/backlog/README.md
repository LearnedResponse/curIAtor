# Backlog

Scoped-but-not-started ideas and work-orders. Each is a candidate, not a commitment; sequencing is
deliberate (ship the Dash-first v0 first, let the example demos surface what to prioritize).

- [**ot-hmi-demo**](ot-hmi-demo.md) — OT / HMI-maintenance flagship example: a "rainbow" HMI over a
  simulated process (MING + Dash) that curiator drags toward High-Performance-HMI / ISA-101 from operator
  feedback; the git log is the build story. Second cross-domain example after `curiator-finance`.
- [**general-app-hosting**](general-app-hosting.md) — post-v0 direction: host *any* framework
  (React/Svelte/static, not just Dash) and *multi-file* apps (directories, not single files), via the
  generic `proxy` mount + directories-per-app + scaffold templates (not per-framework plugins).
