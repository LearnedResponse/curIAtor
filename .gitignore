__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
build/
dist/
.pytest_cache/
.DS_Store
.claude/settings.local.json

# feedback artifacts are runtime state: the SQLite ledger, shots, and generated task/trace files all
# stay local (a legacy app_feedback.json is import-only — see curiator/ledger.py)
feedback/shots/
feedback/task_*.md
feedback/tasks/
feedback/replies/
feedback/app_feedback.sqlite*
feedback/app_feedback.json
docs/demo.gif
!docs/demo.gif

# local-login user store (password hashes) — never commit
.curiator-users.json

# local release/paper evidence snapshots may include machine-local clone paths
release-evidence/
